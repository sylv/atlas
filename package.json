{
  "name": "atlas",
  "version": "0.0.0",
  "private": true,
  "license": "AGPL-3.0",
  "packageManager": "bun@1.3.2",
  "engines": {
    "node": ">=20",
    "bun": ">=1.3.0"
  },
  "scripts": {
    "clean": "rm -rf ./packages/*/{tsconfig.tsbuildinfo,lib,dist,yarn-error.log,.next}",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "@atlasbot/configs": "^10.6.0",
    "@biomejs/biome": "^1.7.2",
    "tsup": "^8.0.2",
    "turbo": "^1.13.3"
  },
  "workspaces": [
    "packages/**",
    "apps/**"
  ]
}